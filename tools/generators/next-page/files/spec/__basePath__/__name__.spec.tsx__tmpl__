import React from 'react'
import { render } from '@testing-library/react'

import <%= Name %> from '<%= depth %>pages<%= basePath %>/<%= name %>'

describe('<%= Name %>', () => {
	it('should render successfully', () => {
		const { baseElement } = render(<<%= Name %> />)
		expect(baseElement).toBeTruthy()
	})
})
